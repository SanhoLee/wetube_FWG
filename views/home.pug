extends layouts/main.pug
include mixins/videoBlock

block potato
    .video
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })