extends layouts/main.pug
include mixins/videoBlock

block potato
    .video
        each video in videos
            +videoBlock({
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })